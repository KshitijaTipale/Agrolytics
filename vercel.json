{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/server/app" },
    { "source": "/((?!api/.*).*)", "destination": "/index.html" }
  ],
  "builds": [
    {
      "src": "server/app.py",
      "use": "@vercel/python"
    },
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
